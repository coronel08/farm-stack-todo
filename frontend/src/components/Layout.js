import React from 'react'

function Layout() {
    return (
        <header className="App-header">
            <h1>Farmstack Demo</h1>
            <p className="text-muted lead">Built Using FastAPI, ,React, Mongo</p>
        </header>
    )
}

export default Layout
